README

To run the experiments enter "python kmeans.py" into the command line. This will run the any .arff files in this folder through the clustering experiments. The graphs will pop up as they are produced.